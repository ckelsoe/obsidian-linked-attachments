# OSV-Scanner suppressions. Every entry must document why the advisory is
# accepted and what would let us drop it. Revisit whenever the dependency tree
# changes (a new lockfile may remove the transitive path entirely).
#
# No active suppressions. The former GHSA-mh99-v99m-4gvg (brace-expansion
# unbounded-expansion DoS) entry was removed once the dependency bump on this
# branch cleared the vulnerable path from the tree: brace-expansion now resolves
# only to 1.1.18, 2.1.4, and 5.0.9, each of which is outside the advisory's
# affected ranges, so osv-scanner matches no instance and npm audit is clean.
